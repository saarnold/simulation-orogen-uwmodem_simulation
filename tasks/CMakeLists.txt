# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(uwmodem_simulationTaskLib)
ADD_LIBRARY(${UWMODEM_SIMULATION_TASKLIB_NAME} SHARED 
    ${UWMODEM_SIMULATION_TASKLIB_SOURCES})
add_dependencies(${UWMODEM_SIMULATION_TASKLIB_NAME}
    regen-typekit)



TARGET_LINK_LIBRARIES(${UWMODEM_SIMULATION_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${QT_LIBRARIES}
    ${UWMODEM_SIMULATION_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${UWMODEM_SIMULATION_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${UWMODEM_SIMULATION_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${UWMODEM_SIMULATION_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${UWMODEM_SIMULATION_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${UWMODEM_SIMULATION_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${UWMODEM_SIMULATION_TASKLIB_HEADERS}
    DESTINATION include/orocos/uwmodem_simulation)

